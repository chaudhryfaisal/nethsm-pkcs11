# Mock PKCS#11 Configuration for Testing
backend:
  type: mock

slots:
  - label: MockSlot
    description: Mock HSM for testing
    operator:
      username: "operator"
      password: "password"
    instances:
      - url: "mock://localhost"
        danger_insecure_cert: true